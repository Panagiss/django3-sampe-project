pipeline {
    agent any


    stages {
        stage('Run ansible playbook for Django') {
            steps {
                sh '''
                    pwd    
                '''
            }
        }
    }
}
